
<div class="title">
    <div class="title-text">See the available Goys in <%= params[:city] %> between <%= params[:starting_date] %> and <%= params[:ending_date] %></div>
    <div class="title-btn">
      <%= link_to "edit search", root_path, class: "btn-gag"%></div>
    </div>
  <div class="row">

      <% @goys.each do |goy| %>
        <%= render 'components/card', goy: goy %>
      <% end %>

  </div>

