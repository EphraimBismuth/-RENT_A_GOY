<div class="title">
  <div class="title-text">
    <h3 class="text-center">See the available Goys in <%= params[:query].capitalize %> </h3>
  </div>
  <div class="title-btn text-center">
  </div>
</div>
<div class="row">
    <% @goys.each do |goy| %>
      <%= render 'components/card', goy: goy %>
    <% end %>
</div>


  <div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
></div>

