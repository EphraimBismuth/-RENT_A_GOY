<%= simple_form_for [@goy, @booking] do |f| %>

  <div class="form-inputs">
    <%= f.input :start_date, as: :string%>
    <%= f.input :end_date, as: :string%>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
